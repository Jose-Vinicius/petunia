<div style="max-width: 800px; margin: 2rem auto; padding: 0 1rem;">
  <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem; flex-wrap: wrap; gap: 1rem;">
    <div>
      <h1 style="font-size: 1.75rem; font-weight: 700; color: var(--text-primary); margin: 0 0 0.5rem 0;">
        <%= t("bank_accounts.index.title") %>
      </h1>
      <p style="color: var(--text-secondary); margin: 0; font-size: 0.95rem;">
        <%= t("bank_accounts.index.subtitle") %>
      </p>
    </div>

    <%= link_to t("bank_accounts.index.new_button"), new_bank_account_path, class: "btn btn-primary" %>
  </div>

  <% if @bank_accounts.empty? %>
    <div style="background: var(--surface-card, #121215); border: 1px solid var(--border, #27272a); border-radius: var(--radius-lg, 12px); padding: 3rem 1.5rem; text-align: center; color: var(--text-secondary);">
      <span class="material-symbols-outlined" style="font-size: 3rem; color: var(--border, #27272a); margin-bottom: 1rem;">account_balance</span>
      <p style="margin: 0 0 1.5rem 0;"><%= t("bank_accounts.index.empty") %></p>
      <%= link_to t("bank_accounts.index.new_button"), new_bank_account_path, class: "btn btn-primary" %>
    </div>
  <% else %>
    <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1.25rem;">
      <% @bank_accounts.each do |bank_account| %>
        <div style="background: var(--surface-card, #121215); border: 1px solid var(--border, #27272a); border-radius: var(--radius-lg, 12px); padding: 1.5rem; display: flex; flex-direction: column; justify-content: space-between; gap: 1rem;">
          <div style="display: flex; align-items: center; gap: 0.75rem;">
            <span class="material-symbols-outlined" style="color: var(--primary, #a78bfa); font-size: 1.5rem;">account_balance</span>
            <h2 style="font-size: 1.2rem; font-weight: 600; color: var(--text-primary); margin: 0;"><%= bank_account.name %></h2>
          </div>

          <div style="display: flex; gap: 0.5rem; justify-content: flex-end; border-top: 1px solid var(--border, #27272a); padding-top: 1rem;">
            <%= link_to edit_bank_account_path(bank_account), class: "btn btn-ghost", style: "padding: 0.35rem 0.6rem; font-size: 0.85rem;" do %>
              <span class="material-symbols-outlined" style="font-size: 1.1rem; vertical-align: middle;">edit</span>
              <%= t("helpers.submit.update", model: "") %>
            <% end %>
            <%= button_to bank_account_path(bank_account), method: :delete, data: { turbo_confirm: t("bank_accounts.destroy.confirm") }, class: "btn btn-ghost", style: "color: var(--error, #ef4444); padding: 0.35rem 0.6rem; font-size: 0.85rem;" do %>
              <span class="material-symbols-outlined" style="font-size: 1.1rem; vertical-align: middle;">delete</span>
              <%= t("account_users.index.remove") %>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
