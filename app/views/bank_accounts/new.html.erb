<div class="auth-container">
  <div class="auth-card">
    <div class="auth-header">
      <h2 class="auth-title"><%= t("bank_accounts.new.title") %></h2>
      <p class="auth-subtitle"><%= t("bank_accounts.new.subtitle") %></p>
    </div>

    <%= form_with(model: @bank_account, local: true) do |f| %>
      <% if @bank_account.errors.any? %>
        <div id="error_explanation" style="background: rgba(239, 68, 68, 0.1); border: 1px solid var(--error, #ef4444); border-radius: var(--radius-md, 8px); padding: 1rem; margin-bottom: 1.5rem; color: #fca5a5; font-size: 0.9rem;">
          <ul style="margin: 0; padding-left: 1.25rem;">
            <% @bank_account.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :name, t("activerecord.attributes.bank_account.name"), class: "form-label" %>
        <%= f.text_field :name, autofocus: true, class: "form-input", placeholder: "Ex: Nubank, Itaú, Bradesco" %>
      </div>

      <div class="form-group" style="margin-top: 1.5rem; display: flex; gap: 0.75rem;">
        <%= link_to t("devise.shared.links.back"), bank_accounts_path, class: "btn btn-secondary" %>
        <%= f.submit t("bank_accounts.new.submit"), class: "btn btn-primary", style: "flex: 1;" %>
      </div>
    <% end %>
  </div>
</div>
